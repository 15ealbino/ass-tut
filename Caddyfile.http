:80 {
    encode gzip

    # Mobile web app (Expo web export) served under /app/.
    @app path /app /app/*
    handle @app {
        reverse_proxy mobileweb:80
    }

    # Everything else — the main web app and its /api proxy.
    handle {
        reverse_proxy frontend:80
    }
}
