fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-jose[cryptography]==3.3.0
python-multipart==0.0.9
pydantic[email]==2.7.1
pydantic-settings==2.2.1
aiosqlite==0.20.0
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0

# Optional: enables the PyGhidra compile method (/compile?method=pyghidra).
# Nuitka brings its own gcc invocation; pyghidra additionally needs a Ghidra
# install on disk pointed at by GHIDRA_INSTALL_DIR plus a JDK on PATH.
# If these are absent the endpoint returns 503 instead of failing on startup.
nuitka==2.4.8
pyghidra==2.1.0
